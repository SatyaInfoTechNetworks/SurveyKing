[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm --prefix Frontend install", "npm --prefix Backend install"]

[phases.build]
cmds = ["npm --prefix Frontend run build"]

[start]
cmd = "node Backend/server.js"
